interface:
  display_name: "自动化 QA / 回测 Agent"
  short_description: "需求驱动的 UI/API/流式/持久化证据回测与判定"
  default_prompt: "Use $automated-qa-test to derive a requirement matrix, audit requirement-source coverage, validate the probe plan, run the QA cycle with UI/API/WebSocket/SSE/command evidence, audit the ledger, generate a strict qa-verdict gate, and produce an evidence-backed QA/backtest report."

policy:
  allow_implicit_invocation: true
